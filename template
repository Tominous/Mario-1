Attivatore istantaneo

        if message.content.lower() == "attivatore":
            triggered = ['risposta 1', 'risposta 2']
            await message.channel.send(
                f"{random.choice(triggered)}")

------------------------------------------------------------------

Attivatore in DM

        if message.content.lower() == "attivatore":
            triggered = ['risposta 1', 'risposta 2']
            await ctx.author.send(
                f"{random.choice(triggered)}")


